#include "main.h"

/**
 * print_diagsums - check the code
 * @a: variable
 * @size: tamano
 * Return: Always 0.
 */
void print_diagsums(int *a, int size)
{
int i;
int b;
b = *a + size;
for(i = 0; i < 6; i++)
{
b++;
}
/*lo que me ensenian: 1+1=2
lo que piden en el examen: Printea la suma de dos diagonales de una
matriz cuadrada de numeros enteros xdd */
}
