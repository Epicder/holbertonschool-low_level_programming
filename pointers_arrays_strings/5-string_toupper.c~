#include "main.h"
/**
 * string_toupper - cambia todas las minusculas a mayusculas
 *
 * Return: Always 0.
 */

char *string_toupper(char *)
{  
int i;
for (i = 0; *[i] != '\0'; i++)
{
if (*[i] >= 'a' && *[i] <= 'z')
{
str[i] = str[i] - 32;  
}
}
return (str);
}
