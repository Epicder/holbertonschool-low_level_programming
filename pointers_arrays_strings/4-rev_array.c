#include "main.h"
/**
 * print_array - printea una array al reves
 * @a: an array of integers
 * @n: the number of elements to swap
 *
 * Return: nothing.
 */
void reverse_array(int *a, int n)
{
return (0);
}
