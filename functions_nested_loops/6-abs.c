#include "main.h"
#include <stdio.h>

/**
 * _abs - check the code
 * @n: la integral que tiene el valor absoluto
 * Return: Always 0.
 */

int _abs(int n)
{
  if (n >= 0)
    {
      return (n);
    }
  return (-n);
}
